#include <stdio.h>
#include "head.h"
void modify_score()
{
    char name[20];
    int id;
    printf("请进行姓名学号进行双重认证：\n");
    scanf("%s", name);
    scanf("%d", &id);
    Node *p = g_head;
    
    
}